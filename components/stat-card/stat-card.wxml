<view class="stat-card rounded-12 bg-white p-24">
  <view class="stat-value text-2xl font-bold text-{{color}}">{{value}}</view>
  <view class="stat-unit text-{{color}}" wx:if="{{unit}}">{{unit}}</view>
  <view class="stat-label text-sm text-gray mt-16">{{label}}</view>
</view>
